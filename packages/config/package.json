{
  "name": "@weaver/config",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Loads Weaver's YAML source contracts and environment settings. The only place that reads configuration from disk.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@weaver/core": "workspace:*",
    "yaml": "2.8.1",
    "zod": "4.1.13"
  }
}
