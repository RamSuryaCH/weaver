packages:
  - packages/*
  - apps/*

# esbuild ships prebuilt binaries via a postinstall script; tsx and vitest
# both depend on it. Everything else stays script-free by default.
onlyBuiltDependencies:
  - esbuild
  - '@libsql/client'
  - libsql
  - sharp
